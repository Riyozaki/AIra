# SALVAGE.md — вердикт по заброшенным линиям исследований (volна 6b)

Метки честности: [измерено] / [проекция] / [якорь] / [отрицательно] / [гипотеза].

Изучены два источника:
- `ovococjsjs-gif/miniLLM` → `/home/user/refs/minillm_ref` (AIra-Qwen / AIra One: адаптация Qwen3.5-0.8B-класса графа, GDN-головы, token-skip, обучаемые state-updater-ы; постмортем `docs/aira-v2-audit.md`).
- ветвь `arena/019fcef3-aira` репо `ovososjdjd-boop/AIra` → `/home/user/refs/aira_brain` (программа EXP01–EXP15: predictive-coding equilibria, σ-δ события, триггер-полка L0, DAM/HDC-эпизодика).

## 1. Что у них сработало (по их же измерениям, без перепроверки — [якорь])

- **Триггер-полка L0 (n-граммная история «контекст→след.символ» + рынок «полка/нейрон»)** — их самый сильный механизм:
  wikitext2 (байтовый уровень) 29.0% покрытия @95% точности, 33.3% @94.6% с иерархией и 2D-гейтом; русский «та же книга» 27.3% @97.2%. Гибрид ×10 дешевле нейронной зоны на активном выводе, ppl не хуже нейронного.
- **DAM/HDC одно-шотовая эпизодическая память**: 16 698 естественных эпизодов, cloze-точность 0.998; чтение-по-сюрпризу на 4.2% позиций дало −2.5% ppl.
- **Методологический комплект безопасности** (принимаем целиком, см. §4).

## 2. Что у них умерло, причины, и почему нам это не трогать

| Идея | Их вердикт | Наш вердикт |
|---|---|---|
| Predictive-coding обучение без BP (ZoneRNN, PC-ALM как референс) | ppl ×2.35 от BP; 74.5% «гейтовых» отказов; шаг PC = 11× стоимости BP при T=32; зазор 5.6–35% даже после ремонтов | **[отрицательно] НЕ переносить.** На плотном CPU локальные градиенты — это всегда лишние матмулы против одного BP; их собственное исследование подтверждает: эффективности там нет, только «биологичность» |
| σ-δ событийное кодирование трафика | помогает только там, где шина и так разрежена (спайки), на плотных матрицах экономии нет | **нишевая, не для наших плотных GEMM.** Для потокового лога состояний (сохранение чекпоинтов/трассировок) — только как формат хранения |
| Жёсткая фильтрация новизны (сhelf-first, боттлнек обучения) | нейронное плечо голодает: ppl 30.7 против 1.13 контроля (H-33, arm A) | **[отрицательно] никогда не резать данные жёстко; только мягкий вес `w_t = floor + (1−floor)(1−p_prior)^γ`, floor 0.10–0.20** (их же ремонт) |
| Псевдо-ppl из уверенности (`−log conf` на попадании, `log V` на промахе) | не является вероятностной мерой; все их «ускорения без проигрыша» пересчитаны впустую | **принимаем как запрет:** любая «качественная» сравнительная метрика у нас — только через нормированное распределение (proper mixture/back-off). Прописано в нашем протоколе |
| Байтовая полка ↔ BPE-токены | совпадения живут ВНУТРИ токенов: 1.28% маршрутизации, мост мёртв | диагноз тот же для нас в обратную сторону: наши токены грубее → см. наш отрицательный опыт §3.1 |
| Автономный байпас (route без учителя) по confidence | 0/18 прогонов прошли гейты; дрейф не лечится одной калибровкой | **[отрицательно]** автономное эскалационное роутирование не строить на их коре; у нас ADR — обученный вместе с моделью, а не поверх неё |

## 3. Наши собственные проверки salvage-кандидатов (LeanCore, реальные прогоны)

### 3.1 Полка на уровне слов — [отрицательно] мёртв
`lc_shelf.py market` (k≤3, Wilson-LB гейты, честный протокол fit-train / θ на первой половине val / отчёт на второй):
```
θ     покрытие@cal  точность@cal  покрытие@test  точность@test
0.50    0.90%     34.3%         0.82%       38.3%
0.8     0.02%     57.1%         0.02%       37.5%
≥0.9    0.00%
```
902 375 контекстов из 1,011,399 токенов train — у словесных контекстов слишком мало повторов, продолжение не доминирует. **`lc_shelf.py: market` зафиксирован как отрицательный результат; больше на словах не пробовать.** (Их 29% были на БАЙТАХ/символах — у нас такого уровня гранулярности нет, а добавлять его только ради полки = новый класс сущностей без бенефита.)

### 3.2 Вечный EMA-контекст поверх T=96 — [отрицательно] нейтраль/хуже
Потоковый прогон ckpt_L_ssa1500, NLL по позициям t∈[48..95] каждого чанка (края не считаем), три режима:
- `reset` (как сейчас): **191.01**
- `cycle` (позиции циклятся, состояния не сбрасываются НИКОГДА): **191.01** — эффект переноса h нейтрален
- `freeze` (позиция замирает на t=95): **194.92** — хуже

Вывод: модель не выучила полезного »дальше-чанка» в h (sc/Wm-масштаб мал; история реально короткая), а pos-вектора жёстко привязаны к [0,T). Длинная память этой ветке архитектурой **запрещена**: нужен rel-pos/NoPE и EMA-ablation — это дорого и вне текущего фронта. Не делать.

### 3.3 erank96 + ss(512, 0.12) @1500 — вердикт финальный [измерено, один сид]
- @1250 по тому же сиду: ssa 117.61 vs er96ssa **137.79** (+17.2% PPL) — разрыв по качеству на длинном горизонте **не закрывается** (на @500 было +11.8%).
- erank = инструмент ИНФЕРЕНСа (срез 49–66% стоимости головы при экспорте), но **в обучении — хуже**. Дорожная карта WAVE6 → T4 порт low-rank головы в inference-стек; из тренера как «ускоритель» — убрать.

## 4. Что принимаем как процесс (из их комплекта безопасности)

1. **Любой «бypass/событийный» механизм** доказывается только: proper-mixture PPL + замеренные tok/s + importance-sampled оценка стоимости — запрет на псевдо-метрики.
2. **Калибровка порогов** — только на отложенной половине валидации, отчёт на второй половине (Wilson-LB, поддержка n; воспроизведено в `lc_shelf.py`).
3. **Мягкие веса** вместо жёсткой фильтрации данных (floor 0.10–0.20), если когда-либо вернёмся к novelty-весам.
4. **Гейты**: burst/per-cycle лимиты на «дешёвое плечо», мониторинг дрейфа калибровки между доменами (их cross-domain дрейф = наша потенциальная prep5k/prep6k).
5. Новые эксперименты — фиксация отрицательных результатов обязательна (эта традиция уже есть в наших wave-доках).

## 5. Остаточные гипотезы (парковка, без бюджета)

- Многотокенный «jump head» поверх потока (их оракул-граница ×7.88 на байтах) — у нас RNN не проверяет спаны дёшево; ждёт появления span-verifier-а.
- Эпизодика-по-сюрпризу (HDC, чтение 4.2%) — только для дистанционного повтора фактов; на нашем корпусе 1M токенов — сигнал подозрителен; требует доказательства принципов (POI: prep-датасет с синтетическими дальними факт-ассоциациями).
